#!/usr/bin/env bash
# Native prerequisites on Windows: the hosted runner already ships
# WebView2 Runtime, MSVC Build Tools, and the Windows SDK.
set -euo pipefail

echo "dsh-launcher CI: Windows prerequisites are preinstalled on the hosted runner."